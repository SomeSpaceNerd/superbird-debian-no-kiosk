# Files to be added to image

data/ (files to be added to data partition while installing debian)
env/ (env config, only switchable is used; includes premade .dump for switchable)
host/ (files to be installed on host raspberry pi)
logo/ (images to pack for superbird boot logos)
system_a/ (files to be added to system_a partition for Utility Mode)
